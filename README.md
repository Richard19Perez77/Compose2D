# Compose2D

Minimal Jetpack Compose Android app for 2D / canvas experiments. Most of the work is in `MainActivity.kt`.

## Stack

Kotlin, Jetpack Compose.

## Run

Open the project in Android Studio and run the `app` configuration.

```bash
./gradlew :app:assembleDebug
```
